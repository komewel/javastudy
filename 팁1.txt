1.JAVA_HOME 변수등록
gitignore.io 무엇을 이그노어 파일로 할지 알려줌
이그노어 파일부터 엶
=================================================================

git 실행할 프로그램 : bash -리눅스 기반 프로그램 

명칭.
~ : 홈 디렉터리
/  : 루트 디렉터리
.  : 현재 디렉터리
.. : 상위 디렉터리
-a: 숨긴파일

해야할일 
1. javastudy 디렉터리를 로컬 레파지토리(저장소)로 등록하기
   cd C:/GDJ61/javastudy
   git init 깃 초기화(=깃 저장소가 되었다)
2. 사용자 등록 git config user.name komewel (얜 파일만) (git config --global user.name pc전체를 관리자로 등록)
                  git config user.email kimcomewel@gmail.com
                  git remote add origin (git hub에서 주소 복사)

3. github 가입

4. github을 이용해 원격 레파지토리(내컴터 로컬레파지토리랑 원격이랑 연결)

5.github에서 personal access token 만들기

6. 로컬 레파지토리에 원격 레파지토리 등록하기(등록한 이름 origin=원격 레파지토리 이름)

7. 로컬 레파지토리에 파일 추가하기

8. 추가된 파일을 추적 시작하기(무대에 올린다 : staging)
   git add 안녕하세요.txt
9. 로컬 레파지토리 상태 저장하기(커밋 : commit)
   git commit -m '커밋메시지'
================여기까지 하면 로컬 레파지토리는 마무리 되고...
10. 로컬 레파지토리의 내용을 원격 레파지토리로 올리기
     github 에서는 수정x 로컬보다 최신화 되어서 원격에서 푸쉬를 거부함
   git push origin(파일네임) main(브랜치) 
명령어
pwd
cd
ls
git init(깃 레파지토리로 만든다)
ctrl+v 강제종료
(shift+insert 리눅스에선 이게 붙여넣기 ctrl+insert는 복사)
**(디렉터리=파일) {pwd치면 홈디렉터리 볼수있는 명령어} cd(change directory / ex. cd ..(상위 디렉토리로 변경해주세요) {ls 파일안에 뭐있는지 보여주는 명령어} {-a 숨긴파일 보여줌}
  {리눅스에선 .으로 시작하면 숨긴파일} {git config user.(.. ) 파일의사용자등록} {git init 깃 초기화(=깃 저장소가 되었다)} {-은 옵션명령어}
.gitignore 파일(git에 올리지 않을 파일), .git파일(내모든이력 로컬레파지토리에 이력들이 다 들어있음)
touch .gitignore 파일만드는 명령어
vi .gitignore(파일명) ->i (메모장 인서트모드)->안올리는 파일명 적기->esc->shift+;->wq(나가기)
이그노어 파일 올리기(git add .gitignore), 지금 진행도상황 보여줌(git status), 리눅스에선 #..이 주석
git add .(내가 따로 이그노어 한거뺴고 다올릴게)
git log --oneline(커밋한이력을 볼수있음 --oneline만큼)
git restore --staged .gitignore ( 스테이징한걸 복원할게 애드한걸 취소한다)
 git reset --hard .....(커밋한걸 취소한다,.....으로 돌아간다)
md파일은 #(띄어쓰기) 하고 약간 설명문 같이 올라감 큰글자로
집에서 작업할때 github 저장된거 가져오고 싶을때 저장하고 싶은 파일에다가 bash 버튼 누르고git clone 원격 레파지토리 주소 붙이고 치면 저장됨
이클립스는 경로가 바뀌면 워크스페이스가 바뀌어서 처음화면같이 됨(파일 불러오기도 해야함 알아서 끌어오기 안됨) import => general => 두번째꺼
